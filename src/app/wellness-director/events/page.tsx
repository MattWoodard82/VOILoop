import { EventsPageContent } from './EventsPageContent'

export const metadata = { title: 'Events and Nudges — VOILoop' }

export default async function WellnessDirectorEventsPage() {
  return <EventsPageContent />
}
